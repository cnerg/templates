% Defines the UW-Madison official university colors for web themes
\usepackage{xcolor}

% UW–Madison’s primary color.
% Used for design elements, navigation bars, nonlinked headers, and backgrounds.
% Not used for links.
\definecolor{uw-red}{HTML}{C5050C}

% Accent color.
% Used in alternating boxes component.
\definecolor{uw-red-dark}{HTML}{9B0000}

% Background color.
\definecolor{uw-page-bg}{HTML}{F7F7F7}

% Background and accent color
\definecolor{uw-gray-blue}{HTML}{DADFE1}

% Design element background color and accent color.
\definecolor{uw-gray-darker}{HTML}{646569}

% Design element background color, accent color, and footer background color.
% Often used in place of black.
\definecolor{uw-gray-darkest}{HTML}{282728}

% Used for standard text on a white background.
\definecolor{uw-body-font}{HTML}{494949}

% Used for links in standard text on a white background.
\definecolor{uw-anchor-color}{HTML}{0479A8}

% Used for footer text.
\definecolor{uw-gray-footer-text}{HTML}{ADADAD}

% Used for links in footer text.
\definecolor{uw-gray-footer-text-hover}{HTML}{F9F9F9}